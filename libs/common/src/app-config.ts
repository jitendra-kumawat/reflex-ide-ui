import { ApplicationConfig } from './types';
import { ERROR_MESSEGES } from './errorMessages';

export const ISOFFLINE = true;
export const TRACE_ROUTES = true;
export const OFFLINE_LOGIN_DATA = { data: undefined, username: '' };
export const appConfig: ApplicationConfig = {
  errorMessages: ERROR_MESSEGES,
 };
