import { AuthInfoResolver } from './auth-info-resolver';

describe('AuthInfoResolver', () => {
  it('should create an instance', () => {
    expect(new AuthInfoResolver(undefined)).toBeTruthy();
  });
});
